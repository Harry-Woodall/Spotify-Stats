export enum PlaylistDetailsState {
  LOADING = "LOADING",
  SUCCESS = "SUCCESS",
  ERROR = "ERROR",
}

export enum PlaylistDetailsErrorType {
  NONE = "NONE",
  TIMEOUT = "TIMEOUT",
  MISSING_IDS = "MISSING_IDS",
}
